# مستندات و گزارش پیش‌پردازش (Preprocessing Report) — گام ۲

**تاریخ:** ۱۱ شهریور ۱۴۰۵
**مسئول:** امیرعلی
**وضعیت:** انجام شده
**اولویت:** معمولی
**ددلاین:** ۳۰ مرداد ۱۴۰۵، ۱۷:۰۰ به وقت ایران

> **نکته:** این فایل قبلاً یک پیش‌نویسِ متفاوت داشت (چک‌لیست مستندات API با
> سبک Sphinx، دیاگرام‌های SVG جدا، راهنمای استفاده) که با متن واقعی تسک
> (تهیه یک گزارش نهایی فارسی، جمع‌آوری نتایج، نمودار، و مقایسه دقت
> IDW/Kriging برای گام ۳) مطابقت نداشت — دقیقاً همان الگویی که قبلاً در
> `task_data_prep_pathfinding.md` و `task_spatiotemporal_consistency.md` هم
> دیده شد. این فایل با نسخه واقعیِ منطبق با تسک جایگزین شده است.

---

## خلاصه

خروجی اصلی این تسک، گزارش نهایی `docs/preprocessing_report.md` است که
نتایج همه ماژول‌های پیش‌پردازش (QC، IDW، Kriging، پیوستگی زمانی-مکانی،
آماده‌سازی داده مسیریابی) را روی داده واقعی خراسان جمع‌بندی می‌کند، به‌همراه
مقایسه دقت IDW/Kriging (leave-one-station-out CV) و سه نمودار واقعی.

## چک‌لیست تسک (Items to Check)

| # | آیتم | وضعیت |
|---|------|--------|
| ۱ | جمع‌آوری نتایج همه ماژول‌های پیش‌پردازش | ✅ |
| ۲ | مستندسازی روش‌ها و پارامترها | ✅ |
| ۳ | تهیه نمودارها و نمونه‌های خروجی | ✅ |
| ۴ | جمع‌بندی دقت روش‌ها (IDW در برابر Kriging) | ✅ |
| ۵ | ارائه گزارش نهایی فارسی | ✅ |
| ۶ | پیوست‌سازی کدها و مستندات | ✅ |

جزئیات کامل: `docs/preprocessing_report.md`

## خروجی‌های تولیدشده

- `docs/preprocessing_report.md` — گزارش نهایی
- `docs/assets/idw_vs_kriging_accuracy.png`, `khorasan_speed_timeseries.png`, `idw_kriging_field_snapshot.png`
- `scripts/generate_preprocessing_report_assets.py` — اسکریپت تولید نمودارها و مقایسه دقت (قابل بازاجرا)

## وابستگی‌ها

- **پیش‌نیاز:** تمام ماژول‌های پیش‌پردازش (QC، IDW، Kriging، Consistency، Data Prep for Pathfinding) — همه روی `main` merge شده‌اند.
- **یافته مهم:** فایل نمونه `data/khorasan_pathfinding_ready.csv` هنوز با داده واقعی پر نشده (تمام مقادیر wind_speed/direction/u/v خالی)؛ در گزارش نهایی به‌عنوان اقدام بعدی برای گام ۳ ذکر شده است.
