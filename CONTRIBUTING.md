# راهنمای همکاری — Roshd Wind Pathfinder

این راهنما برای **همکاران انسانی** پروژه نوشته شده است. اگر شما یک **AI agent** هستید،
فایل [`AGENTS.md`](AGENTS.md) را بخوانید — قوانین مخصوص شما آنجاست.

> خلاصه یکخطی: شما فقط یک توکن (کلید) میسازید و به AI مدلتان میدهید. AI در برنچ
> خودش کار میکند و یک Pull Request (درخواست ادغام) میسازد. **ادغام نهایی فقط با
> تأیید مالک پروژه انجام میشود** — پس حتی اگر AI اشتباه کند، چیزی خراب نمیشود.

---

## مفهوم کلی

این پروژه بهصورت **تسکمحور** در ClickUp مدیریت میشود:

1. هر تسک تعریفشده در ClickUp دارای: عنوان، توضیح (۵ بخش)، چکلیست، ددلاین، اولویت.
2. هر همکار (یا AI نمایندهٔ او) یک تسک را برمیدارد و روی یک **برنچ جدا** کار میکند.
3. در پایان، یک **Pull Request** باز میشود و CI بهطور خودکار تستها را اجرا میکند.
4. **مالک پروژه** (آرمان) بررسی و ادغام (merge) میکند — فقط او.

---

## قدم ۱ — ساخت اکانت گیتهاب

1. به [github.com/signup](https://github.com/signup) بروید.
2. با ایمیل خودتان ثبتنام کنید (یک نام کاربری ساده انتخاب کنید).
3. نام کاربری خود را برای مالک پروژه بفرستید تا شما را به ریپو اضافه کند.

---

## قدم ۲ — ساخت توکن (کلید دسترسی)

توکن یک «کلید» است که به AI اجازه میدهد از طرف شما کد را روی ریپو بگذارد.

1. وارد گیتهاب شوید، روی عکس پروفایل (بالا سمت راست) کلیک کنید → **Settings**.
2. از منوی چپ: **Developer settings** → **Fine-grained tokens** → **Generate new token**.
3. اینها را تنظیم کنید:
   - **Token name:** مثلاً `roshd-ai`
   - **Expiration:** `90 days` (بعد از ۹۰ روز خودش باطل میشود — امنتر است)
   - **Repository access:** گزینه **Only select repositories** → ریپوی
     `roshd-wind-pathfinder` را انتخاب کنید.
   - **Permissions** → بخش **Repository permissions**:
     - **Contents:** `Read and write`
     - **Pull requests:** `Read and write`
     - **Issues:** `Read and write`
4. دکمه **Generate token** را بزنید.
5. **توکن فقط یک بار نمایش داده میشود** — آن را کپی کنید و در جایی امن نگه دارید.

> ⚠️ توکن مثل رمز عبور است. آن را **هیچوقت** در چت، در پیام، در فایل پروژه یا در
> PR نگذارید. فقط به AI مدلتان بهعنوان متغیر محیطی (environment variable) بدهید.

---

## قدم ۳ — دادن توکن به AI مدلتان

به AI مدلتان (مثلاً Claude Code، Cursor، یا هر ابزاری که استفاده میکنید) بگویید:

> توکن گیتهاب من این است: `github_pat_...` (توکن خودتان را جایگزین کنید).
> لطفاً آن را فقط بهعنوان متغیر محیطی `GH_TOKEN` نگه دار و هیچوقت در فایلها یا
> پیامها ننویس.

سپس به AI بگویید این ریپو را کلون کند:

```bash
git clone https://github.com/lawbr3aker/roshd-wind-pathfinder.git
```

---

## قدم ۴ — پرامپت استاندارد

**پرامپت استاندارد پروژه در [`.github/PROMPT.md`](.github/PROMPT.md) قرار دارد.**  
این پرامپت قوانین ۸گانه را برای AI تعریف میکند: خواندن AGENTS.md، خواندن
PROJECT_PROGRESS.md، دریافت تسک ClickUp، استفاده از داده واقعی (نه ساختگی)،
پرسیدن در صورت شک، تست/لینتر، آپدیت مستندات، و PR بدون merge.

استفاده:

1. فایل `PROMPT.md` را باز کنید.
2. توکن خود را در بخش «توکن گیتهاب» قرار دهید (یا به AI بگویید از `GH_TOKEN` استفاده کند).
3. فایل/آدرس تسک ClickUp را کنارش بگذارید.
4. تمام این محتوا را به AI مدلتان بدهید.

---

## قدم ۵ — نحوه کار (گردش کار روزانه)

هر تسک شما در **ClickUp** تعریف شده (عنوان، توضیح، چکلیست، ددلاین). برای هر تسک:

1. به AI بگویید: «تسک من در ClickUp با این عنوان/آدرس است — آن را انجام بده.»
   (آدرس تسک را بدهید.)
2. AI باید طبق [AGENTS.md](AGENTS.md) عمل کند:
   - برنچ میسازد: `task/نام-تسک`
   - فقط چکلیست تسک را پیادهسازی میکند
   - کد را در پوشه صحیح میگذارد (مطابق `docs/AGENT_ROUTING.md`)
   - قبل از ارسال، تستها و لینتر را اجرا میکند
   - یک **Pull Request** میسازد و چکلیست تکمیلشده را داخلش مینویسد
3. **شما کاری نمیکنید** — فقط صبر میکنید. CI (چک خودکار) روی PR اجرا میشود و
   نتیجه را نشان میدهد: ✅ سبز = همهچیز درست است، ❌ قرمز = جایی مشکل دارد.
4. به مالک پروژه بگویید: «PR آماده بررسی است» — او بررسی و در صورت تأیید
   ادغام میکند.

---

## قدم ۶ — اگر PR قرمز شد چه کنیم؟

CI قرمز یعنی تستها یا لینتر خطا دارند. به AI بگویید:

> PR من قرمز است. خروجی خطا را بخوان، مشکل را درست کن و دوباره push کن.

AI باید مشکل را حل کند و دوباره ارسال کند تا سبز شود. اگر نتوانست، از مالک پروژه
کمک بگیرید.

---

## چیزهایی که نباید انجام دهید

- ❌ هیچوقت مستقیم روی `main` کار نکنید یا به آن push کنید (قفل است — عمداً).
- ❌ توکن را در چت، فایل یا پیام ننویسید.
- ❌ PR خودتان را merge نکنید — فقط مالک پروژه این کار را میکند.
- ❌ فایلهای تسکِ دیگری را عوض نکنید؛ هر کس برنچ خودش را دارد.
- ❌ داده ساختگی نسازید — اگر داده لازم دارید، از cache یا API واقعی بگیرید.

---

## سؤالات رایج

**«میتوانم همزمان با دیگری کار کنم؟»**
بله — هر کس برنچ خودش را دارد، کارها تداخل نمیکنند. اگر هر دو یک فایل را عوض
کنید، گیتهاب «Conflict» نشان میدهد؛ در آن صورت AI باید `main` را در برنچ خودش
merge کند و مشکل را حل کند (یا از مالک پروژه کمک بگیرد).

**«از کجا بفهمم تسک من چیست؟»**
از ClickUp. تسکهای شما همانهایی است که اسمتان رویشان است. اگر شک دارید، از مالک
پروژه بپرسید.

**«اگر AI اشتباه کرد چه؟»**
نگران نباشید — چون ادغام فقط با تأیید مالک پروژه انجام میشود، اشتباه به `main`
نمیرسد. PR رد میشود و AI دوباره تلاش میکند.

**«از کجا بفهمم پروژه الان کجاست؟»**
فایل `docs/PROJECT_PROGRESS.md` را بخوانید — بهروزترین وضعیت آنجاست.

**«کد هر تسک باید کجا قرار بگیرد؟»**
فایل `docs/AGENT_ROUTING.md` را بخوانید — جدول مسیریابی کامل آنجاست.